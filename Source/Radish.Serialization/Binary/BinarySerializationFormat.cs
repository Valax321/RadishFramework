namespace Radish.Serialization.Binary;

#if false
internal sealed class BinarySerializationFormat : SerializerBase, ISerializationFormat
{
    private static readonly BinarySerializationFormat Default = new();
    
    public static void Serialize<T>(T obj, Stream outputStream)
    {
        
    }

    public static T Deserialize<T>(Stream inputStream)
    {
        
    }
}
#endif